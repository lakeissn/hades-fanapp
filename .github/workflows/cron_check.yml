name: cron check

on:
  schedule:
    - cron: "*/10 * * * *" # 10분마다
  workflow_dispatch: {}     # 수동 실행 버튼도 추가

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Call Vercel check API
        run: |
          curl -sS -X GET "${{ secrets.VERCEL_CHECK_URL }}" \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
